# YCSB Workload D – Read latest
# 95% random reads (biased toward recently written keys), 5% inserts.
# readwhilewriting: N-1 reader threads + 1 writer thread.

benchmarks=readwhilewriting
read_random_exp_range=0.04
use_existing_db=1
